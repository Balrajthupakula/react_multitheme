import React from "react";

const About: React.FC = () => (
  <section className="page">
    <h1>About Page</h1>
    <p>This is a React app with multiple dynamic themes.</p>
  </section>
);

export default About;